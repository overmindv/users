apiVersion: v1
kind: Secret
metadata:
  name: arcee
type: Opaque
stringData:
  # Example only. Use External Secrets or your cluster secret manager in production.
  JWT_SECRET: "replace-me"
  DATABASE_URL: "postgres://arcee:replace-me@postgres:5432/arcee?sslmode=require"
  BOOTSTRAP_SUPERUSER_EMAIL: "admin@example.com"
  BOOTSTRAP_SUPERUSER_PASSWORD: "replace-me"
